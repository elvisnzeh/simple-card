import React from "react";


const Title = (props) => {
    return(
        <>
        <h1 className="headling">{props.name2}</h1> 
        <h1 className="headling">{props.name}</h1> 
        </> 
    )
}

export default Title;